# Bug修复记录（批次5 Git提交：fix: fix 4 critical production bugs）
> 记录线上模拟出现的4类稳定性故障，包含现象、根因、代码改动点、复现步骤、修复验证方法；后续重构规划写在文档末尾。

## Bug1：告警黑名单接口缺少全局变量alarm_blacklist，调用接口直接抛出 NameError，FastAPI 返回500崩溃
- **现象**
调用 `/api/alarm/blacklist/add` / `/api/alarm/blacklist/remove` / `/api/alarm/blacklist/sta` 接口时服务直接抛出`NameError: name 'alarm_blacklist' is not defined`，FastAPI返回500，告警黑名单相关接口全部不可用，不影响Modbus采集与数据库入库主流程。
- **根因**
业务逻辑设计中需要按从站ID维护告警类型黑名单，接口函数实现完成，但**遗漏全局状态变量定义**；接口函数访问未声明的变量触发Python名字解析异常。多线程场景下同时缺少读写锁保护，若变量存在还会存在并发dict读写竞态。
- **代码改动点**
1. 将`alarm_blacklist`移入`StateManager`类作为实例成员，不再使用松散全局变量：`self.alarm_blacklist: Dict[int, List[str]] = {}`
2. 新增细粒度锁 `_lock_alarm = threading.Lock()`，所有对`alarm_blacklist`读写操作都加锁保护；
3. FastAPI接口全部改为读写`state_mgr.alarm_blacklist`对象，移除独立全局字典。
- **复现步骤**
1. 启动v4.py程序；
2. POST请求调用接口 `POST /api/alarm/blacklist/add`，传入`slave_id=1,alarm_type="temp"`；
3. 观察服务日志，直接抛出NameError异常，HTTP响应500。
- **修复验证**
1. 重新启动修复后程序；
2. 调用add接口添加黑名单，调用sta接口查询，调用remove接口删除黑名单；
3. 接口返回code=0无500报错；并发压测接口，日志无dict并发修改异常，黑名单数据读写符合预期。

## Bug2：MySQL连接池没有连接有效性检测，数据库闪断、长时间空闲后产生僵死失效连接。数据库恢复后，消费线程拿到旧坏连接，数据依旧无法入库
- **现象**
MySQL长时间空闲、数据库重启/网络闪断之后，连接池队列缓存旧TCP连接变为僵死失效连接；当MySQL服务恢复正常，消费线程取出池内旧连接执行executemany，持续入库失败，不会自动创建新有效连接，队列持续堆积，大量数据落入离线JSON缓存。
- **根因**
自定义`MysqlPool`连接池实现，从队列取出缓存连接之后**未做连接存活校验**，直接把可能已经断开的连接返回上层业务；pymysql连接对象不会主动感知TCP断开。
- **代码改动点**
1. 在`MysqlPool.get_conn()`方法，获取到缓存连接后执行`conn.ping(reconnect=True)`；
2. ping内部自动检测连接，失效时底层自动重连；重连失败则丢弃该坏连接，重新新建MySQL连接返回；
3. 在`release_conn`归还连接阶段增加ping探测，失效连接直接close丢弃，不归还放入池队列。
- **复现步骤**
1. 正常启动采集程序，确认数据可以正常写入MySQL；
2. 停止MySQL服务，程序持续采集，消费线程入库失败，数据写入本地json缓存；
3. 等待1‑2分钟后重新启动MySQL；
4. 旧版本：消费线程持续拿到僵死连接，依然入库失败，不会自动恢复；修复版本：ping检测到失效自动重建连接，缓存回放正常入库。
- **修复验证**
1. 重复上面数据库启停操作；
2. MySQL恢复后，消费线程可以快速恢复入库，离线缓存json文件自动回放删除；
3. 观察日志，无持续MySQL连接异常报错；队列可以正常消费下降。

## Bug3：脏寄存器过滤逻辑缺陷，全0脏数据会误判从站下线，产生虚假离线告警
- **现象**
PLC通信链路正常，Modbus TCP请求响应成功，但PLC返回寄存器全部为0脏数据；旧逻辑只过滤寄存器=65535，全0数据会被当作有效数据处理，部分场景统计逻辑异常，错误累加失败计数，触发从站判定下线，打印虚假“从站下线”日志，实际设备网络通信正常。
- **根因**
`filter_dirty_regs()`脏寄存器判断函数仅对`r ==65535`做脏数据判定；PLC设备故障会返回全0寄存器，通信协议层面没有报错，但业务值无效；此时通信本身是成功，不应该累计失败计数、不应该触发设备下线逻辑。
- **代码改动点**
1. 修改`filter_dirty_regs()`函数，检测寄存器列表中存在0 或者65535则判定为脏寄存器；
2. 脏数据场景标记通信status=success，不进入失败分支，不调用`slave_online_monitor`失败计数逻辑，不会触发从站下线状态变更；
3. 脏数据直接丢弃，不投递数据队列，不写数据库，但打印debug日志提示脏寄存器。
- **复现步骤**
1. 使用Modbus仿真工具，模拟从站正常TCP应答，返回寄存器全部为0；
2. 旧版本：采集status成功，但脏数据逻辑放过，持续累积异常，从站状态被判定下线；
3. 修复版本：识别为脏寄存器，丢弃本次采样，不会变更设备上下线状态。
- **修复验证**
1. 仿真返回全0寄存器，观察日志打印脏寄存器debug日志；
2. slave状态保持在线，不会打印“从站xx下线”错误日志；
3. 正常业务数值寄存器可以正常采集入库，不受影响。

## Bug4：离线 JSON缓存存在半写文件、数据丢失风险；回放删除文件缺少异常捕获
- **现象**
数据库不可用时，业务会把待入库数据写入本地JSON缓存。程序被kill崩溃、磁盘占满的场景，json写入中途中断，产生半写损坏JSON文件；下次程序启动加载缓存`json.load()`直接抛出JSONDecodeError，整批离线缓存数据全部丢失；回放成功删除缓存文件时IO异常未捕获，抛出异常干扰启动流程。
- **根因**
1. 直接打开目标cache_file覆盖写JSON，程序崩溃会截断文件，形成非法半写json；
2. 文件IO写异常时，批次数据直接丢弃，没有容错；
3. `os.remove(cache_file)`没有try‑except捕获异常，文件被占用、权限不足会抛出异常。
- **代码改动点**
1. 写入缓存策略：先写入`.tmp`临时文件，写完之后调用`os.replace()`原子替换正式缓存文件，避免半写损坏；
2. 文件读写增加`_lock_json_cache`锁，多线程避免并发写损坏；
3. tmp文件写入发生异常，打印error日志，内存批次不丢弃等待下一轮再次尝试写入；
4. `load_and_replay_to_sql`回放成功后删除缓存文件，增加try‑except捕获`OSError`，记录警告日志，不阻断程序启动流程。
- **复现步骤**
1. 停止MySQL，让程序触发离线缓存写入；
2. 在程序写json缓存的瞬间，执行kill‑9强制杀掉进程；
3. 旧版本：cache文件半写损坏，下次启动解析JSON报错，缓存数据全部丢失；
4. 修复版本：要么完整tmp替换成功，要么保留旧完整缓存，不会出现半写损坏文件。
- **修复验证**
1. 重复kill‑9强制终止程序复现场景；
2. 重新启动，缓存文件可以正常加载回放，不会触发JSONDecodeError；
3. 模拟文件被占用无法删除，仅打印warning日志，程序正常启动运行。

# 后续迭代优化方向
## 已完成迭代（批次6、批次7，代码已落地）
1. 批次6：移除单一大全局锁，按业务域拆分为多把细粒度锁，统计、从站状态、告警、文件IO各自使用独立锁，避免全部业务被同一把锁阻塞，降低线程锁竞争。
2. 批次7：实现StateManager状态管理类，绝大多数业务运行状态、统计计数、设备状态、告警缓存收拢到类内部实例state_mgr，消除大量零散业务全局变量。

## 待后续迭代（仅方案，尚未编码实现）
1. 缩小部分锁的临界区：部分`with lock:`块内部包含非共享变量的计算逻辑，后续把IO、耗时计算移出锁范围，进一步缩短锁持有时间。
2. 将剩余顶层运行时全局对象（DATA_QUEUE、uvicorn_server等）收拢进StateManager，进一步清理顶层全局变量。
