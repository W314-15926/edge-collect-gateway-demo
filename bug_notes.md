# Bug修复记录（批次5 Git提交：fix: fix 4 critical production bugs）
> 记录模拟线上出现的4类稳定性故障，包含现象、根因、修复方案；后续重构规划写在文档末尾。

## Bug1：告警黑名单接口缺少全局变量alarm_blacklist，调用接口直接抛出 NameError，FastAPI 返回500崩溃
- 现象：调用 /api/alarm/blacklist/add / remove / sta 接口时，直接触发NameError，程序返回500，接口不可用。
- 根因：代码中定义了操作alarm_blacklist的接口，但**没有提前声明全局字典alarm_blacklist**，接口函数访问变量时找不到。
- 修复方案：在全局变量区域增加 `alarm_blacklist: Dict[int, List[str]] = {}` 全局字典，用于存储按从站id分组的告警黑名单类型，接口读写该变量，读写时加全局锁保证线程安全。

## Bug2：MySQL连接池没有连接有效性检测，数据库闪断、长时间空闲后会产生僵死失效连接。数据库恢复后，消费线程拿到旧坏连接，数据依旧无法入库
- 现象：MySQL长时间空闲或者数据库重启闪断，连接池内保存的连接变成僵死无效连接；数据库服务恢复之后，消费线程取出旧连接执行SQL，持续入库失败，不会自动换新连接。
- 根因：自定义MysqlPool在get_conn获取连接时，**没有校验连接是否存活**，直接返回队列中缓存的旧连接。
- 修复方案：从连接池取出连接后，先执行一次轻量`conn.ping()`检测连接有效性；ping失败就关闭该坏连接，重新新建数据库连接返回给调用方。

## Bug3：脏寄存器判断逻辑缺陷：仅判断 65535，没有处理全 0 脏数据。Modbus通信链路正常，但PLC返回寄存器全部为0这类无效脏数据；原有过滤逻辑只识别65535，会误处理全0脏寄存器，会错误累积通信失败计数，造成PLC从站误判离线，产生虚假告警。
- 根因：`filter_dirty_regs`脏数据判断函数只判断寄存器值等于65535，缺少全0脏数据判断逻辑。
- 修复方案：修改脏寄存器过滤函数，同时识别两种脏数据：寄存器等于65535 或者寄存器全部为0，满足其一就判定为脏数据，丢弃本次采集结果；同时本次采集标记为成功通信，**不触发从站离线计数**，避免误判设备下线。

## Bug4：离线 JSON 缓存存在数据丢失风险：程序崩溃或磁盘满会出现文件半写损坏；文件 IO 异常直接丢弃内存批次；缓存回放删除文件缺少异常保护。
- 现象：程序崩溃、磁盘空间不足时，json缓存写入过程中断，文件变成半写损坏json，下次加载缓存直接解析失败，离线采集数据全部丢失；回放成功后直接os.remove删除文件，没有捕获删除异常。
- 根因：直接打开目标文件覆盖写入json，没有临时文件中转；IO异常时内存批次直接丢弃；os.remove没有异常捕获。
- 修复方案：采用临时文件方案，先写入`cache.tmp`，写入完成后使用os.rename原子替换原缓存文件，防止半写损坏；写入发生IO异常时，增加重试或者日志留存策略，不直接丢弃批次；缓存回放成功删除缓存文件时增加try‑except捕获删除异常，记录日志。

# 后续迭代优化方案
1. 全局大锁拆分
当前项目全部多线程读写共用一把GLOBAL_LOCK全局锁。高并发采集场景下锁竞争激烈，采集线程、消费线程、http接口线程互相阻塞，采集任务会被数据库IO、文件IO阻塞。
优化思路：按业务域拆分细粒度锁，拆分为：采集状态锁、数据库写入锁、告警状态锁、文件读写锁。不同业务模块使用独立锁，缩小锁的持有范围，减少锁等待时间，提升并发吞吐量，避免单一IO阻塞全部业务线程。

2. 全局状态封装 StateManager
项目当前采集统计计数、从站在线状态、告警时间缓存、窗口统计数据全部散落在顶层全局变量，分散在代码各处。多线程读写分散，状态管理混乱，变量随处修改，容易出现并发读写数据竞争。
优化思路：新建StateManager状态管理类，把所有全局统计、设备在线状态、告警时间窗口、采集指标全部封装到这个类内部。类内部封装状态读写、更新、查询方法，所有外部线程不能直接操作原始变量，只能调用类提供的方法访问状态。内部封装锁，把并发保护收敛到StateManager内部，消除零散全局变量，统一管理设备运行状态。
